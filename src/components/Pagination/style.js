import { styled } from "styled-components";

export const PaginationStyle = styled.div`
  display: flex;
  justify-content: flex-end;
  align-items: center;
  gap: 0.75rem;

  .arrowControllers {
    width: 12px;
    height: 12px;
    cursor: pointer;
  }
`;
